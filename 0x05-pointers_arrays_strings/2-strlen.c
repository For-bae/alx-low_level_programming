#include <string.h>

/**
 * _strlen - returns length of a string
 * @s: The string to return length for
 *
 * Return: Length of string
 */

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
